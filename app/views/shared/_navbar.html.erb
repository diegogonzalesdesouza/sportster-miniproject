<nav class="navbar margin-navbar" role="navigation" aria-label="main navigation">

  <%= link_to :root do %>
  <%= image_tag "image.png", alt: "SPORTSTER", width: 224, height: 56 %>
  <% end %>
  <!-- <div class="navbar-start">
    <div class="navbar-item">
      <div class="buttons">
        <%# <%= link_to "MY LIKES", athlete_interests_path(current_user.athlete) %> %>
      </div>
    </div>
  </div> -->

  <div class="navbar-end">
    <div class="navbar-item">
      <div class="buttons">
        <% if user_signed_in? %>
          <% if current_user.is_athlete %>
            <% if current_user.athlete.nil? %>
              <%= link_to "CREATE PROFILE", new_athlete_path(current_user.athlete), class: "button is-info " %>
            <% else %>
              <%= link_to "VIEW PROFILE", athlete_path(current_user.athlete), class: "button is-info " %>
            <% end %>
          <% elsif !current_user.is_athlete %>
            <% if current_user.brand.nil? %>
              <%= link_to "CREATE PROFILE", new_brand_path(current_user.brand), class: "button is-info" %>
            <% else %>
              <%= link_to "VIEW PROFILE", brand_path(current_user.brand), class: "button is-info " %>
            <% end %>
          <% end %>
          <%= link_to "LOGOUT", destroy_user_session_path, method: :delete, class: "button is-light" %>
        <% else %>
          <%= link_to t(".sign_in", default: "LOGIN"), new_user_session_path, class: "button is-info navbar-item" %>
          <%= link_to t(".sign_in", default: "CRIAR CONTA"), new_user_registration_path, class: "button is-light" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
